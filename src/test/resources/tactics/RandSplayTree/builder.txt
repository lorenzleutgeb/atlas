(w{mono}
(let:tree:cf
  node
  (let:tree:cf
    leaf
    (w{mono} (* size because binds a leaf *)
      (let:tree:cf
        leaf
        (w{mono} (* size because binds a leaf *)
          (let:tree:cf
            node
            (w{mono} (* proof leaf 2 *)
              node
            )
          )
        )
      )
    )
  )
)
)